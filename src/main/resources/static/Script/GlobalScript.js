function showDropDownMenu(id) {
    let DDM = document.getElementById(id);
    if (DDM.style.display === 'block') {
        DDM.style.display = 'none';
    } else {
        DDM.style.display = 'block'
    }
}

function toggleChallengeFriend() {
    let listOfFriends = [[${listOfFriends}]];
    listOfFriends.forEach(function (user){

    });

    function iterate(user) {
        let DDM = document.getElementById('betForm' + user);
        if (DDM.style.display === 'none') {
            DDM.style.display = 'block';
        } else {
            DDM.style.display = 'none';

        }
    }
}